@using DinnerPartyRoa.Models
@model IEnumerable<GroupedOrderViewModel>

<div id="OrderTable">

    <table id="Orders">
        @foreach (var order in Model)
        {
            <tr>
                <td>@order.Quantity</td>
                <td>@order.ItemName</td>
            </tr>
        }
    </table>
</div>